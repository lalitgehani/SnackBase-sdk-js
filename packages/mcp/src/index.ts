/**
 * @snackbase/mcp
 * SnackBase MCP Server entry point.
 */

async function main() {
  console.error("SnackBase MCP Server starting...");
  // TODO: Implement MCP server infrastructure
}

main().catch((error) => {
  console.error("Fatal error during startup:", error);
  process.exit(1);
});
